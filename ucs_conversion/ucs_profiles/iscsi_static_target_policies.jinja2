
  "{{ name }}" = {
    description  = "{{ descr }}"
    ip_address   = "{{ ip_address }}"
    port         = {{ port }}
    lun_id       = {{ lun['lun_id'] }}
    organization = "{{ org }}"
    target_name  = "{{ target_name }}"
{%- if tags %}
    tags = [
  {%- for item in tags %}
      {
    {%- for k, v in item.items() %}
        {{ k }} = "{{ v }}",
    {%- endfor %}
      },
  {%- endfor %}
    ]
{%- else %}
    tags         = []
{%- endif %}
  }