
  "{{ name }}" = {
    description    = "{{ descr }}"
    enabled        = {{ enabled|lower }}
    ipmi_key       = null
    organization   = "{{ org }}"
    privilege      = "admin"
{%- if tags %}
    tags = [
  {%- for item in tags %}
      {
    {%- for k, v in item.items() %}
        {{ k }} = "{{ v }}",
    {%- endfor %}
      },
  {%- endfor %}
    ]
{%- else %}
    tags           = []
{%- endif %}
  }